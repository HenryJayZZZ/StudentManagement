server:
  port: 8080   # Spring Boot 启动端口

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_db?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: cms321321
    driver-class-name: com.mysql.cj.jdbc.Driver
  # 配置 JPA（如果用到了，可以去掉）
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

mybatis:
  mapper-locations: classpath:mapper/*.xml
  # 实体类所在包
  type-aliases-package: com.example.demo.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

